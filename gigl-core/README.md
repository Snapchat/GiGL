# gigl-core

C++/CUDA pybind11 extension modules for [GiGL](https://github.com/snapchat/gigl).

This package contains the compiled native extensions. It is a workspace member of the
main `gigl` package and is built separately via scikit-build-core.

## Building

```bash
uv pip install -e gigl-core/
```
